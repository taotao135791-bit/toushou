# 素材需求文档

用 creative-requirements 技能的标准处理本次素材需求任务。

---

1. 先按技能做前置检查：飞书未连接输出 `[[connect:feishu]]` 引导标记后停止；需要 SocialPeta 参考素材而 MCP 未连接时输出 `[[connect:mcp]]` 并停止。
2. 已连接则先确认项目事实（投放信息、产品边界、素材限制），再按技能标准执行：文档层级 → 图片/视频表格 → 文案与合规 → 检索去重 → 原生附件 → 回读核验 → 按技能格式交付。
