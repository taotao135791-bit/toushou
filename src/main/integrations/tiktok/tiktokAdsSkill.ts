/**
 * Bundled agent skill for the TikTok Ads MCP connection. Installed into the
 * team skill library (userData/skills) when the connection succeeds so the
 * agent knows the tool catalog and the write-confirmation rules. Content is
 * verbatim from the skill document shipped with the feature request.
 */
export const TIKTOK_ADS_SKILL_FILE_NAME = 'tiktok-ads.md'

export const TIKTOK_ADS_SKILL_MARKDOWN = "---\nname: tiktok-ads\ndescription: Use when the user invokes $tiktok-ads, mentions TikTok Ads MCP or TikTok for Business, or asks to inspect or manage TikTok Ads accounts, apps, catalogs, audiences, creatives, reports, Smart+ campaigns, ad groups, or ads, or diagnose non-GMV Max advertising performance.\n---\n\n# TikTok Ads\n\n## Overview\n\nUse this skill for requests that should be answered through the TikTok Ads MCP. The verified catalog covers advertiser and Business Center discovery, apps and events, catalogs, audiences, creative assets, identities, pixels, synchronous reporting, Upgraded Smart+ campaign management, and supported non-GMV Max performance diagnosis. Treat current provider output and live callable definitions as authoritative because account access, allowlists, deprecations, and feature availability can vary.\n\n## Core Rules\n\n- Use the bound TikTok Ads MCP only when its live callable tools are available in the current runtime.\n- Select the narrowest live callable operation that directly answers the request; follow the live interface when it differs from this guide.\n- Prefer a directly registered tool over the dynamic dispatcher when both expose the same operation.\n- Do not invent advertiser, Business Center, campaign, ad group, ad, app, catalog, audience, identity, pixel, product, report, or creative identifiers or statuses.\n- Keep access tokens, authorization codes, refresh tokens, private account data, and other authorization material out of prompts, logs, and final answers. Never ask the user to paste credentials into chat.\n- Do not use shell commands, direct HTTP calls, or hand-written JSON-RPC to recreate, probe, or simulate the MCP server.\n- Treat provider output as evidence. Label model interpretation, calculations, diagnostic synthesis, and recommendations separately.\n- Require explicit user confirmation immediately before any creation, update, status change, deletion, or dynamically dispatched write. Summarize the target, scope, and intended change before asking.\n- Treat an enable or pause operation as an external write and a delete operation as destructive, even when all are exposed through one status tool.\n- Do not share sensitive event data with TikTok. Use TikTok-provided music only within the usage rights reported by the provider and do not imply rights for other media.\n\n## Tools\n\n- `advertiser_info_get`: Retrieve details for one or more advertiser ad accounts.\n- `app_info_get`: Retrieve Events API details for an app.\n- `app_list_get`: List apps associated with an advertiser account.\n- `app_optimization_event_get`: Retrieve available app optimization and conversion events for an advertising configuration.\n- `auth_advertiser_get`: List ad accounts the current authorization can manage.\n- `bc_get`: List accessible Business Centers or retrieve a specified Business Center.\n- `catalog_feed_get`: Retrieve one catalog feed or list feeds under a catalog.\n- `catalog_get`: Retrieve one catalog or list catalogs under a Business Center.\n- `catalog_overview_get`: Retrieve product audit-status counts for a catalog.\n- `catalog_product_get`: Retrieve and filter products in a catalog.\n- `catalog_set_get`: Retrieve one product set or list product sets in a catalog.\n- `catalog_video_package_get`: Retrieve one catalog video package or list video packages under a Business Center.\n- `creative_portfolio_list_get`: List creative portfolios under an advertiser account.\n- `custom_conversion_list_get`: List Custom Conversions associated with a Pixel or App event source.\n- `dmp_custom_audience_list_get`: List owned and shared custom audiences for an advertiser.\n- `dmp_saved_audience_list_get`: Retrieve Saved Audiences associated with an ad account.\n- `file_image_ad_search`: Search image creatives in an advertiser Asset Library.\n- `file_music_get`: List advertiser-uploaded and TikTok library music available for supported ad creation.\n- `identity_get`: List identities under an ad account.\n- `identity_video_get`: List TikTok posts under an identity.\n- `page_get`: Retrieve page records available under an ad account or Business Center form library.\n- `pixel_list_get`: List pixels and associated Pixel Events for an advertiser.\n- `report_integrated_get`: Run a synchronous integrated advertising report.\n- `smart_plus_ad_create`: Create an Upgraded Smart+ ad.\n- `smart_plus_ad_get`: Retrieve Upgraded Smart+ ads in an ad account.\n- `smart_plus_ad_status_update`: Enable, pause, or delete Upgraded Smart+ ads.\n- `smart_plus_ad_update`: Update an Upgraded Smart+ ad.\n- `smart_plus_adgroup_budget_update`: Apply immediate or supported scheduled budget changes to Upgraded Smart+ ad groups.\n- `smart_plus_adgroup_create`: Create an Upgraded Smart+ ad group.\n- `smart_plus_adgroup_get`: Retrieve Upgraded Smart+ ad groups in an ad account.\n- `smart_plus_adgroup_status_update`: Enable, pause, or delete Upgraded Smart+ ad groups.\n- `smart_plus_adgroup_update`: Update an Upgraded Smart+ ad group.\n- `smart_plus_campaign_create`: Create an Upgraded Smart+ campaign.\n- `smart_plus_campaign_get`: Retrieve Upgraded Smart+ campaigns in an ad account.\n- `smart_plus_campaign_status_update`: Enable, pause, or delete Upgraded Smart+ campaigns.\n- `smart_plus_campaign_update`: Update an Upgraded Smart+ campaign.\n- `tiktok_ads_diagnosis_agent`: Diagnose supported non-GMV Max delivery, spend, CPA, scalability, and creative-fatigue issues and return data-grounded recommendations.\n- `tool_execute`: Execute a dynamically discovered TikTok API operation by its exact tool name.\n- `tool_get`: Retrieve the current definition for selected dynamically discovered TikTok API tools.\n- `tool_list`: List dynamically reachable TikTok API tools by object group.\n- Select the narrowest live callable tool that directly answers the request. Follow the live callable interface for current inputs, outputs, and errors.\n\n## Workflow\n\n1. Identify the advertiser or Business Center, target objects, stable identifiers, requested account scope, date range, time zone, filters, metrics, breakdowns, and desired output.\n2. Classify the request as lookup, retrieval, reporting, diagnosis, creation, update, status change, deletion, or another provider operation before routing.\n3. Use `auth_advertiser_get` when an advertiser identifier is required but absent. Use the relevant live lookup to resolve every other missing identifier; never guess.\n4. Prefer a directly registered tool. For a capability available only through the dispatcher, call `tool_list`, fetch only the selected definitions with `tool_get`, classify each definition as read or write, and then call `tool_execute` with the exact discovered name. Obtain confirmation before dispatching a write.\n5. For reporting, preserve provider metric names, dimensions, attribution context, currency, time zone, filters, pagination, and truncation indicators. Batch by supported object identifiers when the synchronous report scope could exceed its provider limit.\n6. Use `tiktok_ads_diagnosis_agent` only for supported non-GMV Max outcome diagnosis, not for generic ads questions, raw rankings, creative generation, billing, policy appeals, edits, or unsupported brand formats. Keep the total selected campaigns, ad groups, and ads below five. Generate a new session identifier for a new diagnosis and reuse it for follow-ups in the same diagnosis conversation.\n7. Before a confirmed Smart+ mutation, retrieve the current object when needed, identify full-replacement or campaign-wide effects from the live definition, and restate any material review, immutability, eligibility, or sibling-impact caveat.\n8. Inspect the outer MCP error and TikTok provider status before reading result fields. Do not treat an empty, partial, or raw response as success without reporting its scope.\n\n## Query Guidance\n\n- Ask only for missing inputs required to choose or safely execute the operation.\n- Keep advertiser accounts, Business Centers, campaigns, ad groups, ads, catalogs, and time windows separate unless the user explicitly requests aggregation.\n- Preserve exact identifiers as strings and state any unambiguous normalization of dates, names, time zones, currencies, metrics, or filters.\n- State report level, metrics, dimensions, attribution context, date range, time zone, filters, sort order, page scope, and data freshness when returned.\n- Do not describe one page, the synchronous reporting limit, or a dispatcher preview as complete unless the provider confirms completeness.\n- Remember that `smart_plus_ad_get` may omit automatically added creatives; use a live operation that explicitly covers all campaign creatives when the user needs a complete creative inventory.\n- For audience results, distinguish owned from shared audiences when the provider returns ownership evidence.\n- For Smart+ changes, verify current eligibility and allowlist status through the live interface rather than assuming availability from documentation.\n\n## Failure Handling\n\n- If no live TikTok Ads callable tool is present, stop MCP execution and report that the connector is unavailable in the current runtime.\n- If OAuth is incomplete or expired, report that browser authorization for the relevant TikTok for Business account is required. Do not request tokens or attempt to bypass authorization.\n- For authorization, account-scope, quota, timeout, invalid-argument, permission, provider, or missing-tool errors, report the failed operation without exposing secrets.\n- Retry at most once only after a safe correction such as resolving a known identifier, narrowing scope, reducing page size, or removing an unsupported optional field.\n- Treat the raw result of `tool_execute` as unvalidated provider output; surface TikTok's error and do not infer success from transport completion.\n- For empty or partial results, report the exact advertiser, object, query scope, page scope, and provider status. Do not fill missing fields from memory or another source.\n- If a requested diagnosis is outside the diagnosis agent's supported scope, state the boundary and route to a narrower live lookup or report only when that operation answers the request.\n- If the user specifically requests TikTok Ads MCP, do not silently substitute another provider. State the connector failure first and offer a clearly labeled fallback only if the user wants it.\n\n## Result Contract\n\n- Separate returned TikTok facts from model interpretation, diagnostic synthesis, and derived calculations.\n- Preserve advertiser and object identifiers, names, statuses, URLs, dates, timestamps, time zones, currencies, units, filters, report level, metrics, dimensions, page scope, totals, and provider caveats when returned.\n- For writes, report the confirmed target, requested change, returned identifier and status, and any partial success or review state. Never claim business success merely because a request was accepted.\n- For diagnosis, state the diagnosed scope and time window, summarize evidence-backed findings and prioritized recommendations, and do not expose internal attribution fields, model details, or sensitive benchmarks.\n- Repeat only the highest-risk provider-specific caveat when it materially affects the result.\n"
