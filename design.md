# 投手产品设计规范

> 文件：design.md
> 规范版本：3.0 · 按用户参考图修订
> 更新日期：2026-09-16
> 源码审查基线：投手 v1.0.0，提交 b0e7456fbbe557fc9431239391c4a46a90ae0a60；实现目标：v1.1.0
> 本次修订范围：视觉、页面交互与可靠性边界；原型仍是独立设计交付物。
> 状态：v1.1.0 实现契约。页面与核心边界已落地；真实 provider 登录、Office 高级对象保真和完整成果生命周期仍按本文限制披露。

## 0. 本次视觉方向，以用户参考图为准

用户选择的方向是：**浅灰侧栏、白色主界面、黑白控件、居中输入框、充足留白，接近原生桌面工具的简洁感。**

用户提供的 Kimi 桌面截图是本次视觉参考。学习其布局比例、控件层级、留白与轻量感，产品名称仍为“投手”，功能来自投手现有能力。不要复制 KIMI 标志、用户名、会员等级、特定模型名称或未实现的集群/深度研究入口。

本版完全取代 2.0 的墨绿侧栏、宋体标题和默认三栏方案。后续开发不再自由选择另一套颜色或首页结构。遇到旧规范与本文冲突，以用户最新参考和本版为准；原有权限与数据保护约束继续有效。

配套原型为同目录 `toushou-design-preview.html`，默认打开新会话首页，包含对话、文件、表格、浏览器、自动任务和插件连接视图。原型使用示例数据，未连接账户、真实业务文件或模型。它展示布局和部分交互；说明弹窗与模拟状态不能直接成为正式产品功能。

如果同时提供概念图片，图片只表达外观方向，**可执行的尺寸与行为以本文及 HTML 原型为准**。生成图不能替代真实应用截图或验收结果。

### 0.1 必须一眼能看出的变化

| 位置 | 目标表现 |
| --- | --- |
| 全局外壳 | 浅灰侧栏与白色主内容；主内容轻微内嵌，薄边框与圆角 |
| 侧栏 | 轻灰 Work / Chat 分段按钮、灰底新建会话、统一线性图标、项目与历史 |
| 首屏中心 | 黑色“投手”字标；宽输入框；输入框下方相连的浅灰项目/插件托盘 |
| 首页快捷入口 | 单行或自然折行的描边胶囊，使用浏览器、文档、表格、素材、自动任务等真实入口 |
| 普通对话 | 侧栏加单一宽对话区；不展示无内容的右侧占位板 |
| 浏览器与 Office | 打开实际工作对象后才展开工作区，沿用统一黑白工具栏；保存、版本与归属依然清楚 |
| 页面排版 | 全部使用系统无衬线字体；标题写“文件”“自动任务”等功能名称 |

只改颜色变量、增加一个设计文档，或仅修改截图，均不满足本轮验收。需要实际修改外壳、导航、首页、输入框和工作区组合。

### 0.2 对“高级”的具体约束

1. 靠对齐、间距、字重和内容秩序获得品质感。
2. 首页的视觉中心只有品牌与输入框；功能入口是次级信息。
3. 常规导航、选中、按钮均使用中性灰。绿色、红色、琥珀色仅用于必要业务状态；文件格式图标允许小面积类型色。
4. 不使用大面积有色侧栏、装饰渐变、发光边框、玻璃层、悬浮球和星光图标来代表 AI。
5. 不使用宋体大标题、口号式主标题、营销文案墙、四宫格功能卡或首页 KPI 仪表盘。
6. 不为“有设计感”增加第二套顶栏、重复工具栏和遍布页面的大卡片。

## 1. 产品定位与使用规则

投手是以广告工作为核心、兼顾通用任务的桌面 Agent。用户围绕客户或项目，通过对话、浏览器、文件、连接器和自动任务完成持续工作。

用户应能理解当前对象和数据范围、知道正在做哪一步、检查来源、打开并修改成果，以及在中断后从明确位置继续。简化外观不能删除这些必要反馈。

涉及权限、运行时、文件访问、协作和发布，遵循当前 AGENTS.md、扩展宿主契约与 Main 实际权限规则。本文不授予额外权限，不覆盖现有 FB 只读限制。

复用当前组件、路由和主题变量；通过兼容映射演进。不得用第二套运行时、静态 iframe 或孤立的假首页替代真实产品改版。

### 1.1 设计原则

- **对象明确。** 当前项目、网页、文件和版本有稳定身份，切换视图不会悄悄变更任务目标。
- **结果真实。** 执行结束、验证通过、保存和送达有各自含义。
- **过程可继续。** 登录、权限、失败和重启保留进度与输入，提供准确恢复入口。
- **成果可操作。** 文件能打开、定位来源、继续修改和导出。
- **呈现克制。** 重要状态直接可见，技术细节按需展开，空白无需填满。

### 1.2 设计完成标准

用户不需要理解 MCP、IPC、schema、sessionId 或磁盘 grant，也能回答：当前在处理什么、做到哪一步、结果是否可靠、文件是否保存，以及下一步怎么做。

技术细节放入可展开诊断区域，不占据主输入流程。

## 2. 信息架构

### 2.1 侧栏结构

| 层级 | 入口及规则 |
| --- | --- |
| 窗口区 | 系统窗口按钮与侧栏切换；留出真实拖拽区域 |
| 视图切换 | Work / Chat；可本地化为“工作 / 对话”，控制界面呈现 |
| 主要动作 | 新建会话，浅灰填充，右侧显示实际可用快捷键 |
| 常用入口 | 浏览器、文件、表格、自动任务、插件与连接；仅展示可用能力 |
| 项目 | 项目选择或新建项目入口；默认允许本地工作区 |
| 对话 | 最近会话；超长标题单行省略并能查看完整名称 |
| 底部 | 设置、搜索、外观与本地/账户身份 |

Work / Chat 是同一工作上下文的两个视图，不建立两套会话系统。Chat 展示首页或当前对话；Work 恢复当前工作对象。没有工作对象时，Work 展示实际文件/任务入口或诚实空状态，不能自动创建假文件。切换不销毁编辑器，不重置浏览器，不启动新模型任务。

正式实现保留已有看板、插件、Skill、历史和深链接可达性。低频能力可以收入“更多”或插件页，不必照参考图把每种能力都放在侧栏。先接真实路由再收敛展示。

### 2.2 上下文层级

项目/客户 → 会话或自动运行 → 网页/文件等工作对象 → 成果及版本。

- 首页项目入口位于输入框下方托盘；会话中可在上下文标签或面包屑查看，不重复铺满页面。
- 无项目可使用本地工作区。需要客户数据时，再明确作用范围。
- 新任务默认沿用当前项目；已有任务的项目归属不能被一个全局选择器静默改写。
- 从通知、历史、文件进入时定位准确的项目、会话和对象；内部使用稳定 ID，不能靠同名匹配。
- 切换项目不销毁后台运行或未保存文档。

## 3. 桌面布局

### 3.1 三种工作状态

| 状态 | 布局 |
| --- | --- |
| 新会话首页 | 浅灰侧栏 + 完整白色内容面；主内容不显示面包屑顶栏或空工作面板 |
| 对话中 | 侧栏 + 单列对话；正文和输入框在宽工作面内居中，顶部保留简单会话标题 |
| 打开文件或浏览器 | 侧栏 + 对话 + 工作对象；允许展开对象、返回对话，窄窗改为视图切换 |

首页居中指内容面内居中，不包含侧栏。禁止为了给工作区预留空间，把首页输入框压在左半屏。

### 3.2 1440 × 900 的落地尺寸

参考截图存在显示缩放，按结构和比例转换，不把截图每个像素机械照搬。

| 元素 | 默认值 | 自适应规则 |
| --- | --- | --- |
| 侧栏 | 260px | 约 1200px 窗口收至 228px；保留用户宽度偏好 |
| 外壳内边距 | 主内容顶部、右侧约 8px | Electron 原生窗口已有圆角时避免叠出厚边框 |
| 主内容圆角 | 18–20px | 1px 极浅分隔线，无厚外阴影 |
| 窗口按钮区域 | 约 50px 高 | 按系统原生按钮位置留白，不遮挡红绿灯 |
| Work / Chat 控件 | 42px 高 | 外圆角 12px，选中白底，轻微阴影 |
| 新建会话 | 44px 高 | 外圆角 12px；填充 #EEEDEB |
| 导航行 | 39–42px 高 | 图标 19–20px，行距 5–8px |
| 首页内容宽 | max-width 880px | 两侧至少约 38–48px，紧凑视口按比例缩小 |
| 首页顶部留白 | 约 18vh，限制 80–175px | 优先保证输入框与主要入口完整可见 |
| “投手”字标 | 62px / 1.24，700 | 紧凑窗口 48–56px；下间距 29–32px |
| 主输入区 | 约 150px 高 | 白底、25px 圆角；随输入增长到上限后内部滚动 |
| 下方上下文托盘 | 最少 56px 高 | #F7F7F7，与输入框外轮廓连接；可换行 |
| 发送按钮 | 38px 圆形 | 空输入浅灰禁用，有内容黑底白箭头 |
| 快捷入口 | 38–42px 高 | 1px 边框，胶囊形状；与托盘间隔 34px |
| 普通对话宽 | 约 760–830px | 正文阅读宽度约 640–760px，输入框与内容对齐 |
| 展开对象后的对话 | 350px | 紧凑窗口 310px，宽屏可 400px；保留调节能力 |
| 对象工作区 | 使用剩余空间 | 可用宽度不足约 520px 时切换单栏，不强塞三栏 |

1440px 窗口下默认首页为 260px 侧栏 + 约 1170px 主内容；输入组合约 880px 宽。打开工作对象后，对话约 350px、对象约 820px。1024px 窗口使用“对话 / 工作区”切换，保留对象状态。

布局恢复必须根据可用宽度约束旧值。修复 localStorage 空值被转换成 0 的问题，区分无偏好与合法偏好。默认首页不读取旧的工作区宽度来挤占中心区域。

### 3.3 滚动、调整与窗口集成

- 对话、表格和网页使用稳定滚动容器，正文与主要按钮不横向溢出。
- 工作区分栏支持拖拽、键盘调整与恢复默认，命中范围不能只有 1px。
- 隐藏/展开是视图操作，不等于关闭文档或销毁 WebContentsView。
- 对象只保留一套主要标题栏和工具栏，不重复显示同名标题与相同按钮。
- macOS 使用真实窗口控件，Windows 使用系统适配；原型的红黄绿圆点仅用于视觉说明，不能作为正式伪窗口按钮。
- `-webkit-app-region: drag` 只覆盖可拖拽空白，所有交互控件必须 `no-drag`。

## 4. 视觉系统

### 4.1 方向

中性、浅色、简单。侧栏与主界面只通过轻微底色差分开，主按钮接近黑色，所有标题与导航采用无衬线字体。正文、文件和真实任务内容是界面的主要视觉元素。

深色模式使用中性石墨灰，跟随同一层级；不延续上一版墨绿色。默认展示浅色，不能以深色截图代替本轮主要验收。

### 4.2 颜色 token

| Token | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| bg.app / bg.sidebar | #FAF9F8 | #1B1B1B | 外壳、侧栏 |
| bg.surface | #FFFFFF | #232323 | 白色主工作面 |
| bg.paper | #FFFFFF | #282828 | 文件预览 |
| bg.soft | #F7F7F7 | #2C2C2C | 托盘、气泡、辅助区域 |
| bg.hover | #F1F0EF | #303030 | 轻 hover |
| nav.selectedBg | #EEEDEB | #303030 | 当前导航、新建会话、分段底轨 |
| border.subtle | #E8E7E6 | #383838 | 非必要装饰边界 |
| border.control | #DCDCDC | #505050 | 输入框与控件边界 |
| border.focus | #8C8C8C | #AFAFAF | 输入框聚焦边界 |
| text.primary | #202020 | #EDEDED | 标题、正文、导航 |
| text.secondary | #6E6E6E | #AEAEAE | 辅助文字 |
| text.placeholder | #767676 | #A0A0A0 | 占位文案，不能替代可访问名称 |
| action.primary | #202020 | #EDEDED | 主按钮与激活发送 |
| action.primaryText | #FFFFFF | #202020 | 主按钮文字 |
| action.disabled | #D7D7D7 | #4C4C4C | 空输入发送按钮 |
| focus.ring | #202020 | #EDEDED | 键盘焦点，2px 外描边 |
| status.success | #38664B | #A2BAAB | 必要成功状态的小图标/文字 |
| status.warning | #866021 | #DAC393 | 待处理 |
| status.warningBg | #F8F2E7 | #383229 | 待处理的小面积背景 |
| status.error | #AE403B | #E6A5A0 | 明确失败 |

统一映射现有主题变量，禁止在页面内继续硬编码旧主题色。普通已保存标签可以保持灰色，“失败 / 待处理”才需要额外视觉强调。背景色与浅边框不承担唯一的可操作提示；标签、图标、形状、hover 与可见焦点一起帮助辨识。

普通文字对比度至少 4.5:1，必要控件边界与焦点按对应标准检查。参考 [W3C 文字对比度说明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)。在实际背景、透明度、hover 和主题上计算，不仅检查调色板。状态必须包含文字，不能只依赖颜色。

### 4.3 排版

字体统一使用系统 UI 无衬线栈，例如 `-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`。可以保留仓库现有 Geist/Inter，但不新增网络字体依赖。宋体/衬线不用于首页、侧栏、工具栏和示例报告的默认标题。

| 用途 | 字号 / 行高 | 字重 |
| --- | --- | --- |
| 首页品牌字标 | 62px / 1.24 | 700 |
| 普通页面标题 | 24–26px / 34px | 600 |
| 区域标题 | 16–18px / 26px | 600 |
| 首页占位输入 | 18px / 1.65 | 400 |
| 正文与导航 | 14–15px / 22–24px | 400–500 |
| 常规按钮 | 13–14px / 20px | 400–500 |
| 次级信息与状态 | 12px / 18px | 400 |

不通过英文全大写、连续粗体、过度字距和金句文案制造“品牌感”。关键状态不小于 12px。金额/指标使用 tabular-nums、明确币种与单位，缺失值不能显示为零。长文件名有完整查看方式。

### 4.4 间距、形状与图标

- 间距基于 4、8、12、16、24、32、48px，按组调整，不为每个页面单独定义一套。
- 普通按钮 8px 圆角；新建会话与卡片约 10–12px；主输入框 25px；主画布 18–20px；菜单约 12px。
- 圆角大小服从控件尺寸，不把所有东西做成厚胶囊。首页快捷入口与发送按钮是主要例外。
- 图标统一线性、约 1.6px 视觉线宽、16–20px，导航图标约 19px。素材入口用画笔，不用装饰性星光符号。
- 输入框阴影最多是轻微的 `0 3px 5px #00000003, 0 9px 18px #00000003`。侧栏行无悬浮大阴影。
- 正式菜单是靠近触发按钮的 Popover；原型部分入口使用居中对话框简化实现，不能直接照搬所有弹窗。

### 4.5 动效

常规 hover/focus 120–180ms，面板切换不超过约 220ms；只过渡必要属性。不要全局 `transition: all`，不要让工具事件反复推动整页；遵循减少动态效果设置。

## 5. 首页与任务发起

### 5.1 首屏组成

从上到下只有“投手”字标、输入组合、快捷入口。底部最多一条弱化的继续对话入口。历史主要放在侧栏；没有历史就留白，不补假内容。

首页不出现欢迎口号、能力介绍副标题、示例 KPI、四宫格功能卡、升级促销或巨大客户工作区名称。常用项目与插件已经放在输入框下方，不在顶部重复。

### 5.2 输入组合

- 输入区域白底，示例占位：“有什么想交给投手？”；左下角加号，右下模型下拉与圆形发送按钮。
- 文件/网页等引用出现在输入区域内或邻近位置，展示名字与范围，支持移除；不能只用彩色图标代表已附加对象。
- 下方浅灰托盘显示“选择项目”和“插件”。插件一词后可以显示少量真实文件类型图标，但不做能力宣传。
- 模型显示当前实际配置；当前模型不在列表时仍要展示可理解的已保存值。原型的“自动选择 / 深入思考”只是演示，正式产品沿用可用配置。
- 空白输入且无可发送附件时禁用发送；输入内容后激活。附件是否可单独发送按现有产品能力决定，不能随意清掉有效附件。
- 发送时转换为正常对话，保留项目、附件和文本；未调用成功时不伪造答复。流式执行中在同一位置显示停止操作。
- 失败保留原稿和引用，显示具体原因与重试入口。

### 5.3 入口行为

快捷入口实际打开浏览器、已有文件、导入/新建文件或对应工作流。未实现能力不展示“已可用”。不为贴近参考图加入虚构的 PPT、设计、研究、集群等能力。

打开一份文件后才出现工作区；关闭可视工作区回到宽对话，未保存稿仍受生命周期和离开保护管理。不要把“收起工作区”当成“丢弃文件”。

Work / Chat、项目切换、新建会话分别具有明确语义；快捷键提示与真实绑定一致。原型采用 ⌘ / Ctrl + Shift + O 新建、⌘ / Ctrl + K 搜索；正式实现若已有快捷键，复用并更新标签，避免为模仿截图造成冲突。

## 6. 对话与任务状态

### 6.1 对话布局

- 普通对话使用宽单列；只有打开工作对象时才进入分栏。
- 用户输入和智能体回复保持清晰的阅读顺序。
- 当前阶段、等待操作和成果直接可见；工具调用原始详情默认折叠。
- 回复引用数据时能展开来源、数据日期与读取范围。
- 用户向上阅读时停止自动滚到底；有“回到最新”按钮。
- 流式内容不会反复重排已显示区域；长表格、代码和引用有稳定容器。
- 从浏览器或 Office 发起询问默认使用当前会话；新建会话是明确的用户动作。
- 发送失败保留文字、附件引用与队列，用户可编辑后重试。

### 6.2 任务状态与文案

| 状态 | 界面说明 | 主要操作 |
| --- | --- | --- |
| 准备中 | 正在准备本次任务 | 取消 |
| 执行中 | 正在读取报表 / 分析数据等具体阶段 | 停止、查看详情 |
| 等待用户 | 请登录后继续 / 请确认这处修改 | 完成当前所需操作 |
| 受阻 | 缺少报表权限 / 文件无法访问 | 去修复、选择其他来源 |
| 已停止 | 已停止；列出已完成内容 | 查看结果、按条件继续 |
| 失败 | 失败步骤、已保留内容与具体原因 | 重试此步骤、编辑输入 |
| 执行结束 | 展示答复或成果，以及验证与交付状态 | 打开、核查、继续修改 |

“运行时 idle”只能触发运行状态核对，不能直接决定所有业务结果均成功。

数据验证、文件保存、外部交付独立展示。例如：

> 报告已生成并保存。飞书发送失败，可重新发送。

不能用单一绿色“已完成”覆盖仍失败的交付要求。纯答复任务的完成标准按答复质量和来源判断，不强制生成文件。

### 6.3 停止、排队与恢复

- 停止操作立即有反馈，并停止新的自动动作；等待 Main 确认实际取消状态。
- 已发生的外部副作用按记录展示，不能暗示停止会自动撤销。
- 后续排队消息暂停并保留；失败消息可恢复为草稿。
- 登录或连接恢复后定位到原任务、原阻塞步骤；继续前重新核对页面/文件状态。
- 应用重启后从持久状态恢复；无法恢复的任务标记中断并给出下一步。
- 所有迟到响应都核对运行代次和工作对象，不能改变已经切换的新任务。

## 7. 浏览器工作区

### 7.1 顶部工具栏

左侧：后退、前进、刷新/停止。
中间：地址与页面标题。
右侧：当前登录环境、由谁操作、交给智能体、展开、关闭。

每项图标有可读名称和提示。高频动作留在工具栏，清理登录环境、下载位置等进入详情菜单。

登录环境使用“客户 A 的浏览器”等可理解名称；内部 partition、contextId 和 token 不显示在主界面。

### 7.2 操作归属

| 场景 | 必须发生的行为 |
| --- | --- |
| 交给智能体 | 绑定当前页面、当前会话及必要选区；显示作用对象 |
| 由我操作 | 暂停自动输入，让用户接管；界面说明当前由用户操作 |
| 交还智能体 | 重新观察页面；旧引用失效后继续 |
| 另一个任务需要浏览器 | 明确排队、独立页面或接管选择，不静默导航抢占 |
| 打开弹窗 | 将其作为可识别的页面目标，明确当前操作窗口 |
| 页面已变化 | 重新读取后执行，不能沿用过期元素编号 |

持久登录环境和本次执行页面分开管理。保留现有登录数据；用户可以主动共享或隔离不同客户的环境，不默认读取系统浏览器私人数据。

### 7.3 读取与验证

- “看当前页面”引用真实页面状态；标题和 URL 不替代内容读取。
- 可以引用选区或截图区域，清楚显示读取对象与范围。
- 密码和凭据不进入普通页面快照、日志或模型上下文。
- 页面加载失败、内容不可访问、定位过期和权限不足使用不同说明。
- 导航请求发出、页面加载完成、目标动作成功是不同结果。
- 未知 iframe、虚拟列表或不支持的页面结构明确说明范围，并提供重新读取或导入文件等实际出口。
- FB 的现有只读规则继续生效。需要切账户、日期或视图时，引导用户手工完成，再重新读取。

FB 报表展示账户、统计日期、采集时间、币种和已知数据口径；日期、必要汇总或行覆盖不足时显示“范围待确认”。两次读取一致不能单独产生“完整验证通过”。

### 7.4 下载与上传

下载显示：
准备 → 下载中 → 完成 / 失败 / 已取消。

完成后登记实际成果，提供“打开”“分析这个文件”“查看位置”。下载总量未知时不显示虚构百分比。名称冲突、权限错误与取消保留明确结果。

上传先提供用户选文件的受控流程；只有实际支持并验证的工具能力才显示自动化入口。不以一个按钮暗示可上传任何文件到任何页面。

### 7.5 原生 Electron 交互

WebContentsView 可能覆盖 React 层，CSS z-index 不是验收依据。

命令面板、菜单、确认框、文件对话框与网页的遮挡、焦点和恢复由统一机制管理。打开浮层时网页不能截获点击；关闭后恢复正确的 bounds、可见性、焦点及操作归属。

在真实 Electron 中验收分栏拖拽、窗口缩放、系统缩放、弹窗和输入法。

## 8. Office 与文件工作区

### 8.1 文件身份与展示

顶部固定展示：
- 文件名与格式；
- 已保存 / 有未保存更改 / 正在保存 / 保存失败；
- 当前工作表或预览页；
- 询问、保存、另存为、版本及展开操作。

用户可打开版本详情；内部 documentId 和 revision 用于准确绑定，不作为主要文案。

文件原件、编辑模型和显示预览分开管理。不能因为预览不支持某种特性，就在保存时悄悄移除它。

### 8.2 读取范围

提供三个入口：
- 询问选中区域；
- 询问当前工作表；
- 询问整个工作簿。

输入框显示文件名和范围。Agent 获取对应编辑版本的真实数据，包括未保存内容。大表按范围读取和计算，界面可以说明当前分析覆盖了哪些行列。

数据包含必要的原始值、显示值、公式、列类型和单位。空值、零、文本编号、日期与百分比不得混淆。

### 8.3 自然语言修改

每个提议显示：

| 内容 | 要求 |
| --- | --- |
| 目标 | 文件、工作表、准确范围 |
| 内容 | 真正的旧值和新值；公式区别展示 |
| 原因 | 简短说明为什么修改 |
| 影响 | 修改数量、结构/公式影响、冲突 |
| 操作 | 应用修改、调整要求、取消 |

一次有意义的“应用修改”完成确认与应用，不在两个页面重复相同确认。

应用前核对文档与版本。提议生成后用户已修改目标区域时，提示冲突并重新计算，禁止应用到另一个同名文件或 sheet。

批量修改作为可撤销的整体；部分失败不能悄悄留下半份结果。应用后显示“有未保存更改”，落盘成功后才显示“已保存”。

### 8.4 未保存内容与离开

打开其他文件、切换工具、切项目、关闭外层面板、应用退出均使用同一保护规则。

有未保存内容时，对话框显示：
- 标题：“保存对《文件名》的更改？”
- 操作：“保存并继续”“放弃更改”“取消”。
- 取消与 Escape 均留在当前内容。
- 保存失败留在对话框或当前文档，并保留修改和目标位置。
- 多文件退出提供统一列表，不连续弹出无法辨认对象的确认框。

可以保留本地恢复草稿；恢复草稿不等于覆盖原文件。用户再次打开时明确区分磁盘版本与恢复版本。

保存过程中继续编辑，成功反馈只对应保存时的版本，后续修改仍是未保存。外部程序已修改原件时，显示冲突，提供保留双方内容的处理方式。

### 8.5 格式支持边界

| 格式 | 首轮目标 | 必须说明的边界 |
| --- | --- | --- |
| XLSX | 读取、计算、选区修改、支持范围内保真保存 | 公式、样式、合并、隐藏表、图表、宏等分别声明支持程度 |
| CSV | 类型明确的读取、分析与导出 | 编码、分隔符、前导零、日期转换、无样式 |
| DOCX | 模板生成、实际文件预览、明确范围的继续修改 | 复杂导入排版和修订的支持范围 |
| PPTX | 可编辑内容生成、实际逐页预览、指定页修改 | 复杂导入模板、动画、媒体等支持范围 |
| PDF | 阅读、页定位、引用、文档导出 | 任意 PDF 的无损编辑不作为默认能力 |

不支持保真保存的导入文件采用只读或明确转换副本，保护原件。不能只在保存之后才告知内容丢失。

预览来自实际产物，标记对应版本。生成后重新读取必要结构；渲染或验证失败时明确显示，不能用单独绘制的 HTML 冒充真实文件预览。

字体、转换器或渲染依赖缺失时给出准确说明和支持的恢复入口，避免把普通用户留在空白页面。

## 9. 成果系统

### 9.1 成果卡

至少包含：
- 文件名、格式与适当图标；
- 所属项目与任务；
- 当前版本和更新时间；
- 验证状态；
- 来源入口；
- 打开/预览、继续修改、导出或查看位置。

卡片来自实际登记的文件或明确类型的成果对象。模型文字里出现路径不等于文件已存在。

纯文本成果可以保存为看板或文档；动作名称要说明结果类型，不将对话中的任意消息默认登记为文件。

### 9.2 版本与来源

- “继续修改”保留成果身份并产生新版本。
- 版本列表说明时间、修改摘要和对应任务。
- 引用来源可打开准确网页、文件范围或数据快照；统计日期与采集时间分开。
- 用户外部移动或删除文件后，卡片说明实际状态，并提供重新定位或重新生成。
- 文件验证至少区分未检查、通过、发现问题、无法验证；“文件可打开”不等于“所有业务内容正确”。

### 9.3 默认交付反馈

| 真实情况 | 文案示例 |
| --- | --- |
| 文件已生成，尚未保存到用户选择的位置 | 已生成，可预览并选择保存位置 |
| 保存已成功 | 已保存 |
| 正在发送 | 文件已保存，正在发送到指定位置 |
| 发送失败 | 文件已保存，发送失败 |
| 无法确认是否收到 | 送达状态待确认 |
| 已有成功回执 | 已送达 |

各阶段使用真实证据。重试发送不重新生成成果，也不重复读取整份数据。

## 10. 自动任务

任务列表优先展示：
名称、项目、启用状态、下一次运行、上次结果、交付状态和待处理项。

详情展示：
任务说明、时区、数据窗口、Skill、授权数据源、固定交付目标和运行记录。

规则：
- “每天 09:00”同时显示所用时区。
- 下一次执行时间与报表统计窗口分别设置。
- 按钮冷却不代替真实运行状态。
- 启动失败及时结束记录，允许立即重试。
- 正常终态及时推送至 UI，不依赖刷新或重开页面。
- 同一次运行的历史、会话和成果使用稳定身份关联，改名后仍可追查。
- 任务依赖缺失时明确受阻，不静默丢弃 Skill 或替换客户来源。
- 飞书等目标固定绑定在任务中，不随最近聊天变化。
- 交付结果未知时先核对回执或让用户确认，避免盲目重复发送。
- 重启后核对未完成运行；无法继续的标记中断，不长期保留假“运行中”。

## 11. 扩展与连接

### 11.1 状态分层

| 状态 | 用户能理解的含义 |
| --- | --- |
| 未安装 | 尚未添加这项能力 |
| 需要登录/授权 | 需要连接账户或补充权限 |
| 服务不可用 | 当前无法访问对应服务 |
| 工具未就绪 | 已连接，但本任务需要的操作尚未可用 |
| 可用 | 当前会话可以使用所需能力 |

连接卡说明“能做什么、当前缺什么、下一步怎么做”。技术 endpoint、scope 和工具列表放在详情中。

绿色“可用”必须依据实际能力检查；握手成功而工具发现失败时不能显示完整可用。Skill 引用不存在的工具或资源时应标记问题。

### 11.2 回到原任务

连接引导保留：
原任务、阻塞步骤、所需能力、输入摘要和已完成工作。

操作顺序：
查看缺项 → 去连接/登录 → 检查当前会话能力 → 继续此任务。

无法向原会话动态注册工具时，采用运行时支持的恢复方式并带回上下文，明确告知用户。连接成功不能自动重新执行状态不确定的外部写入。

## 12. 组件与通用交互

优先扩展现有组件；下列名称表示语义职责，可按仓库现有命名实现。

| 组件职责 | 共用规则 |
| --- | --- |
| ViewSegment / ComposerTray | Work / Chat 只切换视图；托盘明确项目与能力范围 |
| Button / IconButton | 主次层级、焦点、禁用原因、加载反馈和可读名称 |
| ContextChip | 显示项目/文件/网页及范围，可移除和打开 |
| TaskStatus / RunStep | 真实阶段、等待原因和恢复动作 |
| SourceReference | 来源身份、日期、范围和打开方式 |
| ArtifactCard | 实际成果、验证、版本与继续修改 |
| CapabilityCard | 能力就绪与补齐入口 |
| EditProposal | 原值/新值、范围、冲突和应用结果 |
| SaveGuard | 所有离开路径统一保护 |
| SplitPane / WorkspaceTabs | 保留对象状态、可调整尺寸、焦点明确 |
| EmptyState / InlineError | 原因、具体下一步、保留输入 |
| CommandPalette / Dialog | 键盘可达、焦点管理、原生网页遮挡协调 |

组件至少设计 default、hover、focus、active、disabled、loading、success、error 的适用状态。

### 12.1 错误与文案

错误说明包含：
发生了什么 → 哪部分已保留 → 可以做什么。

示例：
- “保存失败，更改仍在当前文件中。请检查目标文件夹，或选择另存为。”
- “当前页面没有完整日期范围。请在浏览器中切换后重新读取。”
- “报告已保存，飞书发送失败。可以重新发送此版本。”
- “这份修改基于较早版本。当前表格已变化，请重新检查差异。”

避免只有“未知错误”“操作失败”“请重试”。内部错误码放详情，并保留诊断入口。

toast 用于复制成功等短反馈。保存失败、权限不足和交付失败必须在对象附近持续可见，直到解决或明确关闭。

### 12.2 键盘、焦点与输入法

- 保留 Cmd/Ctrl+K 命令面板。
- 文档获得编辑焦点时，Cmd/Ctrl+S 保存当前文档；其他区域避免误触文档操作。
- Enter 发送遵循用户设置，Shift+Enter 换行；IME composition 期间 Enter 不发送。
- Escape 优先关闭最上层浮层，不应直接丢弃编辑或停止所有任务。
- Dialog 有焦点约束和关闭后回焦；菜单支持键盘选择。
- 禁用按钮若原因不明显，应有邻近说明，不能只靠 hover tooltip。
- 流式输出不逐 token 触发读屏播报；播报重要阶段和结果。
- 所有仅图标操作提供 accessible name，选中状态与错误不只用颜色表达。

## 13. 前后端状态与界面契约

本节约束界面需要的真实状态，不要求更换 OMP 或建立第二套运行时。

| 界面需要 | 实现必须保证 |
| --- | --- |
| 当前任务不会串线 | 持久 runId、会话身份和项目身份；迟到事件检查代次 |
| 页面操作不会抢占 | browser context/tab 与 owner 绑定，观察版本可失效 |
| 文件修改不会错位 | documentId、revision、授权引用和 patch 前置条件 |
| 保存文案准确 | 捕获版本、落盘结果与当前编辑版本分别记录 |
| 完成状态可靠 | 运行、验证、保存、交付独立记录和组合展示 |
| 重连不丢状态 | 初始快照、事件顺序/版本与重新核对 |
| 连接可以继续任务 | pending action 保存原步骤和能力要求 |
| 成果可打开 | 实际文件存在、类型、授权、所属对象与版本 |
| 错误可恢复 | 稳定错误类别、用户文案、已完成步骤与允许的恢复动作 |

Renderer 展示状态并发起受控操作；Main 负责权限、文件和浏览器归属、持久状态与校验。不要让局部组件布尔值成为这些事实的唯一来源。

扩展和 Skill 必须对接实际可调用工具。界面不能因为安装了说明文件就宣称能力已具备。

## 14. 三条体验闭环

### 14.1 广告报表复盘

当前客户与统计日期明确；用户选择授权来源或导出表。系统说明字段和覆盖范围，生成可编辑表格与汇报文档。用户预览实际文件，提出局部修改，再保存同一成果的新版本。

验收重点：缺失数据不会假验证；来源可查；修改作用于准确文件和版本；保存结果真实。

### 14.2 竞品素材分析

使用真实已配置来源检索，保留素材与证据链接，形成可查看和改写的需求单。按既有授权发送到用户选择的位置。

验收重点：缺来源时提供连接或导入入口；不能编造检索结果；发送失败保留成果并独立恢复。

### 14.3 每周自动汇报

绑定客户、时区、统计窗口、Skill 和交付目标。调度后完成读取、分析、文件验证、保存与发送。用户能打开历史运行、成果与送达状态。

验收重点：启动失败可立即重试；重启后可追查；不因发送失败重跑全部工作；不确定回执不盲目重复发送。

## 15. 界面与交互验收

在真实支持平台执行桌面验收。普通浏览器测试不能替代 WebContentsView、系统对话框和原生焦点验证。

### 15.1 视口与模式

- 至少覆盖当前最小支持窗口约 1024×700，以及 1440×900、1920×1080。
- 浅色、深色、合理系统缩放和减少动态效果。
- 长中文、长文件名、长错误、空列表、大表格与多成果。
- 鼠标、键盘、输入法和读屏必要状态。

### 15.2 必须通过的行为

- [ ] 当前项目、网页、文件和成果身份始终明确。
- [ ] 窄窗口可以完成任务，不丢主要操作或未保存内容。
- [ ] 工作区拖宽、展开、切换与恢复保持对象状态。
- [ ] 浏览器人工接管后智能体停止输入；交还前重新观察。
- [ ] 两个任务不会静默抢占同一页面。
- [ ] 原生网页不会遮住命令面板、确认框和关键错误。
- [ ] 下载完成后有可打开、可继续处理的真实文件。
- [ ] Office 读取包含真实选区及未保存版本。
- [ ] 修改提议显示准确旧值、新值、范围与冲突。
- [ ] 文件 A 的提议无法应用到文件 B。
- [ ] 所有离开路径保护未保存内容；取消不会丢稿。
- [ ] 保存期间继续编辑，后续更改仍显示未保存。
- [ ] 不支持保真保存时保护原件并提前说明。
- [ ] 成果预览对应实际文件及当前版本。
- [ ] 任务终态及时更新；启动失败可以立即重试。
- [ ] 验证、保存和送达状态能够独立检查。
- [ ] 连接补齐后回到准确步骤，不要求重述整个任务。
- [ ] 发送失败只恢复发送；不确定回执避免盲目重发。
- [ ] 关键文本、按钮、焦点和边界在实际背景上清晰可读。
- [ ] 中文输入法、键盘快捷键与表格编辑不互相干扰。
- [ ] 错误保留输入与进度，并提供具体恢复操作。
- [ ] 三条业务闭环均有可重复验收证据，mock 边界明确。

## 16. 实施与维护

推荐顺序：
1. 读取用户参考图和本文，截取当前首页作为基线，列出本轮真实组件改动。
2. 先完成可见的外壳、侧栏、首页、输入框和宽对话，交付同尺寸截图。
3. 在同一套界面接入真实工作区，并修复阻碍流程的数据、任务、身份、保存与离开保护问题。
4. 完善浏览器/Office、连接恢复和独立交付；能力支持边界如实反馈。
5. 完成三条业务路径、窄窗、深色和目标平台验收。

本轮用户的首要反馈是视觉没有变化。不能持续增加后台工作，最后只提交 design.md 而没有真正修改界面。可靠性修复保留在计划内，不能因视觉改版被删除。

设计规范可以先确定，界面实现随实际能力接通。未实现能力不能用假数据、永久 loading 或无效按钮占位后宣称完成。

每次设计变更同步维护：
- 本文中受影响的行为和 token；
- 实际页面与公共组件；
- 有意义的回归验证；
- 真实界面截图及其运行环境；
- 已实现范围与仍未验证的限制。

建议在 AGENTS.md 的界面工作约定中引用：

> 修改产品界面、交互流程或状态文案前，先阅读根目录 design.md；实现与文档须同步，权限和协作仍遵循现有规则。

### 16.1 与现有代码对接

| 文件或模块 | 本轮实际改动 |
| --- | --- |
| src/renderer/index.css 及现有主题映射 | 新颜色、文字与控件 token、深色模式、限定动效 |
| src/renderer/components/Sidebar.tsx | 浅灰导航、Work / Chat、灰底新建会话、项目与历史层级 |
| src/renderer/components/Layout.tsx、src/renderer/App.tsx | 白色内嵌主画布，首页/宽对话/对象分栏三态，保留历史入口 |
| src/renderer/components/ChatPanel.tsx | 居中字标与输入组合、胶囊入口、宽单列对话、阶段与真实文件卡 |
| src/renderer/components/Composer.tsx | 白色圆角输入框、相连浅灰上下文托盘、引用、模型、发送/停止/失败状态 |
| src/renderer/components/WorkspacePanel.tsx | 对象打开后才展示、关闭回宽对话、展开/窄窗切换与稳定生命周期 |
| BrowserPage、OfficePage | 单一工具栏、操作归属、版本、保存、提议与差异 |
| TasksPage、ConnectionsPage | 真实业务状态和针对性的恢复动作 |
| 新成果视图及必要共享组件 | 读取真实 Artifact/文件元数据，保留看板与既有能力 |

不要把整个 HTML 原型作为 iframe 嵌到产品里。将其视觉结构拆进现有 React 组件，接上当前 Main/preload/OMP 契约。原型里的弹窗说明、示例数据、模拟成功状态不能进入正式功能。

### 16.2 视觉验收证据

首页截图必须逐项核对：

- [ ] 首次打开是白色新会话首页；没有默认右侧工作区。
- [ ] 侧栏浅灰；Work / Chat、灰底新建会话、项目与对话层级明确。
- [ ] 黑色无衬线“投手”在主内容内居中，输入框约 880px 宽，整体留白接近参考比例。
- [ ] 白色圆角输入框与下方浅灰托盘形成同一组合，项目与插件入口位于托盘。
- [ ] 发送是圆形按钮，空输入禁用；胶囊快捷入口只有细边框和单色图标。
- [ ] 没有深色侧栏、宋体标题、口号、四宫格功能卡、装饰星光或首页 KPI。
- [ ] 实際主导航、模型列表、插件和文件入口接入已有能力；不是静态假首页。
- [ ] 发起对话后仍是宽单列，点开对象才展开工作区；关闭工作区保留状态。


提交相同视口下的前后对照：首页、工作中、Office、浏览器、成果、自动任务、连接管理。至少提供 1440×900 的浅色与深色截图，以及 1024px 窄窗下的真实操作记录。

把“视觉实现完成”和“业务能力已接通”分开登记。允许先完成一个真实纵向页面，但必须明确剩余范围，不能用文档替代实际成果。

仓库基线可查看 [v1.0.0 审查提交](https://github.com/taotao135791-bit/toushou/commit/b0e7456fbbe557fc9431239391c4a46a90ae0a60)。后续代码变化时更新本文基线与实际支持矩阵。
