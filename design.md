# 投手界面与工作区设计契约

## 产品定位

投手是一个面向广告与通用项目工作的桌面 Agent：对话负责目标、判断和可审查的过程，浏览器与 Office 是当前会话旁边的真实工作面，成果必须能打开、核查、继续修改。界面优先表达“现在在处理什么、完成到哪一步、下一步需要谁确认”，不把模型文本或临时路径当成已交付成果。

## 主导航与页面职责

- 工作台：当前项目、最近会话、主输入框和四个可执行入口；没有项目或连接时也能开始基础任务。
- 对话：流式回答、工具过程、阻塞/确认状态和可恢复输入。停止、等待输入、失败和继续是不同状态。
- 成果：真实文件或可打开对象的名称、类型、版本、来源和验证状态；打开、预览、继续修改是主操作。
- 自动任务：下一次运行、上次结果、失败/阻塞步骤、送达状态和运行详情；“启动中”不等于“已完成”。
- 扩展与连接：Skill、插件和连接器的能力状态、缺项与修复入口，不伪造工具已就绪。
- 浏览器 / Office：右侧工作区的同一对象保持标题、位置和当前会话关联；切换标签不主动销毁未保存内容。

## 关键流程

1. 用户选择项目或无项目开始 → 提交目标 → 查看阶段和工具证据 → 得到真实成果或明确阻塞 → 继续、重试或人工处理。
2. 浏览器先导航再 snapshot；snapshot 返回 `snapshotId`、`tabId`、`observedAt`。click/type 使用同一快照标识，页面变化后重新观察；登录、密码和 Ads Manager 写操作保持在用户可见的受控边界内。
3. Office 原件通过 Main 的一次性授权打开。打开、修改、保存和离开共享同一 dirty/revision 生命周期；保存期间继续编辑只保留对应版本为已保存，后续版本仍显示未保存。
4. Agent 修改采用“提议 → 显示旧值/新值 → 校验文档身份与 revision → 批量应用 → 明确尚未保存 → 用户 Save as 落盘”的流程。冲突或部分失败必须保留可恢复说明。
5. 定时任务生成持久 `runId`，以真实 session 终态结束；启动失败、超时、取消、中断、Skill 缺失和正常完成分别记录，并允许失败后立即重试。

## 工作区布局

- 桌面起点：左侧项目/会话约 232px，中间对话保持约 560px 的可读宽度，剩余空间给工作区。
- 右侧工作区可拖拽调整宽度（320–720px），偏好写入 renderer 的 localStorage；键盘焦点在分隔条上时用左右方向键调整。
- 1100px 以下工作区覆盖到右侧并保留明确关闭按钮，避免三栏被压缩到不可用；浏览器原生 WebContentsView 只在 Browser 标签激活时显示。
- 每个对象只保留一套主要标题和操作栏。工作区标签数量受控：Browser、Office、扩展工具三项固定入口，当前对象状态在各自工具栏表达。

## 视觉 token

- 色彩：浅色 app `#FAF9F7`、侧栏 `#F4F3F0`、表面 `#FFFFFF`、主文字 `#1C1B19`；深色使用语义对应的墨色表面。陶土色 `#D97757` 只做焦点、强调和大面积按钮，不用作小号普通文字。
- 字体：系统无衬线优先，CJK 使用系统中文字体回退；正文 14–15px，导航和按钮 13–14px，辅助信息 12px。关键状态不使用 10px 浅灰。
- 间距：只使用 4 / 8 / 12 / 16 / 24 / 32px 档位。控件圆角约 8px，卡片 12px，主输入区 16px。边框使用低对比度语义线，阴影只用于浮层。
- 动效：反馈通常 120–180ms，只允许 opacity、transform、具体颜色和阴影属性；禁止全局 `transition: all`。尊重 `prefers-reduced-motion`。
- 可访问性：普通文字目标对比度至少 4.5:1；按钮有可见焦点、图标有 title/aria-label、状态不只靠颜色表达；表格和工作区能通过键盘访问。

## 组件状态与错误恢复

- 按钮：默认、hover、focus、disabled、busy 分开表达；busy 期间显示实际操作的 spinner，不以短暂冷却伪装完成。
- 数据加载：loading、ready、empty、partial、failed、retryable 分开；截断显示数量/范围和原因，不能把部分结果标成完整。
- Office：文件名、`v<revision>`、未保存标记始终可见；保存失败保留内容与目标并给出重试/另存为路径；离开、换文件和关闭都确认放弃修改。
- 自动任务：运行记录包含开始、状态、结束、session/history 身份和恢复提示；历史入口按稳定 UUID 打开，不按标题或时间猜测。
- 连接：区分已安装、已授权、在线、握手成功和本会话工具已注册；缺少能力显示具体补齐动作。
- 文案避免未知精确百分比。用户能看到“已完成的步骤、当前动作、失败原因、下一步”。

## Electron 特殊规则

Renderer 不持有任意文件路径、令牌或通用命令能力。文件读写经过 Main 的 native dialog / one-shot grant；原生 WebContentsView 的 bounds、可见性、焦点和 React 浮层由统一布局状态协调。Overlay 出现时必须隐藏或重算原生 view，不能只依赖 z-index。应用退出、面板关闭和重载都清理浏览器 view、运行时 session 和一次性授权。

## Office 能力边界

当前支持 XLSX / CSV 的读取、文本/数值/布尔编辑、导出为 XLSX / CSV、公式文本与计算值保留、常见字体/填充/对齐/数字格式、合并、隐藏表、常见行列尺寸。限制包括 20MB 文件、受控行列/单元格数量和 32,767 字符单元格上限。图表、图片、宏、外链、复杂主题与高级对象不保证保真；打开时的 warnings 必须可见，原件不会被静默覆盖，Save as 生成用户选择的新目标。

## 浏览器限制与恢复

浏览器使用持久的应用分区，不自动读取系统私人 profile。只允许 http(s) 导航和内置白名单动作；页面快照是有限文本/元素投影，密码字段不进入普通快照。快照过期要求重新 snapshot；加载失败返回失败而非成功。Facebook Ads Manager 保持只读：可导航、观察、截图和滚动，禁止通过 Agent click/type 改预算、出价、投放或发布。

## 验收视口

每次界面改动至少检查浅色/深色、1280×800、1440×900、1024×768 和窄窗口覆盖工作区；检查键盘焦点、中文输入法、长内容、空/失败/未保存状态。真实 Electron 回归应覆盖首页、运行中任务、浏览器、Office 修改/保存失败和任务失败后重试；缺少外部登录或 provider token 时，记录为未测而不是伪造通过。
